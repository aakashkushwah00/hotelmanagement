<div  class="our_room" id="rooms">
         <div class="container">
            <div class="row">
               <div class="col-md-12">
                  <div class="titlepage">
                     <h2>Our Room</h2>
                     <p>Lorem Ipsum available, but the majority have suffered </p>
                  </div>
               </div>
            </div>

            <div class="row">
            
               <% @rooms.each do |room| %>
               <div class="col-md-4 col-sm-6 d-flex ">
                  <div id="serv_hover"  class="room">

                     <div class="room_img">
                        <%# <%= image_tag room.room_photo %>

                     </div>
                     <div class="bed_room">


                        <h1><%= image_tag  room.room_image %></h1>
                        <h3>Room Price : <%= room.price %> </h3>
                        <h5>Room_type : <%= room.room_type %></h5>
                        <h5>Floor_Number : <%= room.floor_number %></h5>
                        <h5>Capacity : <%= room.capacity %></h5>

                        <%# <%= link_to 'Edit', url: '/hotels/:hotel_id/rooms/:id/edit', class: 'btn btn-primary' %>
                        <%# <%= link_to 'Delete', url: '/hotels/:hotel_id/rooms/:id/edit', class: 'btn btn-primary' %>

                        
                     </div>

                  </div>
               </div>
                  <% end %>
               
              
               
               
            </div>
         </div>
      </div>